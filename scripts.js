var radius = prompt("Please enter Radius: ");

var height = prompt("Please enter Height: ");

const Pi = 3.14;

var volume = parseInt(radius) * parseInt(radius) * 
parseInt(height) * Pi;

alert("Cylinder volume is: " + volume)
